import { CommandMenu } from '../../components/command-menu';

const Dashboard = () => {
    return (
        <div className="">
            <CommandMenu />
        </div>
    );
};

export default Dashboard;